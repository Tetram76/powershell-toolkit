name: Unit tests

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  pester:
    name: Pester (${{ matrix.os }})

    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest

    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      actions: read
      checks: write
    steps:
      - uses: actions/checkout@v4

      - name: Install Pester
        shell: pwsh
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = 'Stop'
          # ubuntu-latest : PowerShellGet n'a plus PSGallery ; PSResourceGet si.
          Install-PSResource -Name Pester -Scope CurrentUser -TrustRepository

      - name: Run Pester
        shell: pwsh
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = 'Stop'
          Push-Location $env:GITHUB_WORKSPACE
          try {
            & "$env:GITHUB_WORKSPACE/tools/Invoke-Tests.ps1" -Coverage
          }
          finally {
            Pop-Location
          }

      - name: Publish Pester test report
        uses: dorny/test-reporter@v2
        if: (success() || failure()) && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.fork == false)
        with:
          name: Pester Tests (${{ matrix.os }})
          path: TestResults.xml
          reporter: java-junit
          fail-on-error: false

      - name: Write job summary (Pester)
        if: success() || failure()
        shell: pwsh
        run: |
          Set-StrictMode -Version Latest
          $ErrorActionPreference = 'Stop'
          @'
          ## Résultats Pester (${{ matrix.os }})

          - **JUnit XML :** `TestResults.xml` (racine du workspace)
          - **Couverture JaCoCo :** `coverage.xml` (si généré par `Invoke-Tests.ps1` avec `-Coverage`)
          - **Rapport de tests (check / annotations) :** étape *Publish Pester test report* (`dorny/test-reporter`)
          - **Fichiers bruts téléchargeables :** artefacts **pester-test-results-${{ matrix.os }}** et **pester-coverage-${{ matrix.os }}**
          '@ | Add-Content -Path $env:GITHUB_STEP_SUMMARY -Encoding utf8

      - name: Upload test results (JUnit)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pester-test-results-${{ matrix.os }}
          path: TestResults.xml

      - name: Upload code coverage (JaCoCo)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: pester-coverage-${{ matrix.os }}
          path: coverage.xml
