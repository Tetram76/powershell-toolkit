# TODO:
# - $RepoRoot = (Resolve-Path -LiteralPath (Join-Path $PSScriptRoot '..')).Path (tests à la racine => un parent jusqu’à la racine repo)
# - Test-ModuleManifest puis Import-Module Join-Path $RepoRoot 'Tetram.Media.Similarity.psd1' ; structurer par exports du .psm1.


Describe 'Tetram.Media.Similarity (stub)' {

    It 'Stub — tests à ajouter' -Skip {
    }
}
