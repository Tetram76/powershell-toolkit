# TODO:
# - $RepoRoot = (Resolve-Path -LiteralPath (Join-Path $PSScriptRoot '..' '..')).Path  (tests/Utils => deux parents)
# - Test-ModuleManifest sur Join-Path $RepoRoot 'Utils/Tetram.Media.AudioUtils.psd1'
# - Import-Module ensuite ; découper Describe par domaine, It avec jeux minimaux, mocks/ffmpeg si besoin


Describe 'Tetram.Media.AudioUtils (stub)' {

    It 'Stub — tests à ajouter' -Skip {
    }
}
