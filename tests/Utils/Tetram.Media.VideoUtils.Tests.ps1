# TODO:
# - $RepoRoot = (Resolve-Path -LiteralPath (Join-Path $PSScriptRoot '..' '..')).Path (tests/Utils => deux niveaux jusqu’à la racine repo)
# - Test-ModuleManifest / Import-Module Utils/Tetram.Media.VideoUtils.psd1 puis Describe progressivement avec mocks/ffmpeg si pertinent.


Describe 'Tetram.Media.VideoUtils (stub)' {

    It 'Stub — tests à ajouter' -Skip {
    }
}
