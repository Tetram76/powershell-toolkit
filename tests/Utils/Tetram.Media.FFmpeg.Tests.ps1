# TODO:
# - $RepoRoot = (Resolve-Path -LiteralPath (Join-Path $PSScriptRoot '..' '..')).Path
# - Tester le manifest Utils/Tetram.Media.FFmpeg.psd1 puis Import-Module
# - Couvrir ffmpeg présent/absent, erreurs prévues, mocks sans réseau ; structurer Describes par comportement métier.


Describe 'Tetram.Media.FFmpeg (stub)' {

    It 'Stub — tests à ajouter' -Skip {
    }
}
