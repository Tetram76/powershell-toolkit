# TODO:
# - $RepoRoot = (Resolve-Path -LiteralPath (Join-Path $PSScriptRoot '..' '..' '..')).Path ; Import-Module du psd1 racine
# - InModuleScope 'Tetram.Media.Reencode' puis tests sur Streams.ps1 avec IO/ffmpeg mockées si pertinent.


Describe 'Streams (stub)' {

    It 'Stub — tests à ajouter' -Skip {
    }
}
