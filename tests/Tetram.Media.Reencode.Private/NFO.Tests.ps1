# TODO:
# - $RepoRoot = (Resolve-Path -LiteralPath (Join-Path $PSScriptRoot '..' '..' '..')).Path
# - Import-Module Tetram.Media.Reencode.psd1 depuis $RepoRoot ; InModuleScope 'Tetram.Media.Reencode' car NFO.ps1 est dot-sourcé (pas standalone).


Describe 'NFO (stub)' {

    It 'Stub — tests à ajouter' -Skip {
    }
}
